// src/environments/environment.prod.ts

export const environment = {
  production: true,
  // OJO: Esta URL es un EJEMPLO.
  // La URL real la obtendrás cuando despliegues tu backend en Google Cloud Run.
  // Tendrás que venir a este archivo y reemplazarla por la URL que te dé Google.
  apiUrl: 'https://portfolio-api-xxxxxxxx-uc.a.run.app/api/v1'
};
