// src/environments/environment.ts

export const environment = {
  production: false,
  // Esta es la dirección de tu API de Spring Boot corriendo en tu máquina local.
  // Asegúrate de que el puerto (8080) y la ruta base (/api/v1) coincidan.
  apiUrl: 'http://localhost:8080/api/v1'
};
